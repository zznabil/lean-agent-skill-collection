# Lean Agent Skills V8.1.0 — Considerate Agency

V8.1.0 is a nonbreaking collaboration-quality release. It keeps all 23 V8 skills and adds no route.

## Added

- global considerate-agency doctrine;
- ACT / ASK / DO NOT ACT initiative calibration;
- warm, calm, competent user-facing tone;
- explicit NO ACTION NEEDED / DECISION NEEDED / OPTIONAL FOLLOW-UP labels;
- long-horizon babysitting reduction and a bounded teammate pass;
- implementation stewardship and first-use release readiness;
- decision-ready defaults in planning and grilling;
- human-friction review lane;
- reconstruct-free handoffs;
- local skill and OpenAI-adapter reinforcement;
- audit passes 15 and 16 with 60 static scenarios.

## Unchanged

- 23 canonical skills;
- 17 implicitly selectable skills and 6 manual-only skills in Complete;
- no dependency, service, hook, executable, or automatic trusted-state mutation.

Live host behaviour and human satisfaction remain unmeasured.

## Integrity and verification

The supplied all-collections source archive SHA-256 is `22b98bab4bbaf4544ce39f7d3969783ebc11b999465ddc9cafd885c0524ccc38`. Its 19-file manifest passed 19/19 checks, and every nested archive passed path, duplicate-name, and executable-file screening before extraction.

The repository builder generated six profiles and one master archive twice in PowerShell 7 and twice in Windows PowerShell 5.1. All 12 top-level build files were byte-identical across runtimes. Both environments passed validator self-tests and all five static source/package validation groups. Generated assets use the repository's deterministic ZIP layout and are not claimed to be byte-identical to the supplied distribution archives.

The generated profile hashes are recorded in [`CHECKSUMS.sha256`](CHECKSUMS.sha256), with metadata in [`RELEASE-MANIFEST.json`](RELEASE-MANIFEST.json).

## AI provenance

The collection decisions are AI slop chosen by GPT-5.6 Sol Pro. This describes their origin, not their quality or correctness. Review the policy and test it in the target host and project before relying on it.
