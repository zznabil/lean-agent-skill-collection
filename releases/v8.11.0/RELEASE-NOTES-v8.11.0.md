# V8.11.0 — Default communication kernel

## Change

Add the default communication kernel from PR20. The kernel applies response-weight matching, concise external output, quiet completion, explicit blockers, and distinct summary/TL;DR use without changing the collection's task routes.

Retain the V8.10.1 Quick Mode metadata and Boolean-contract guarantees. The package contains source, validation records, notices, and generated archives; it does not bundle a runtime, installer, or host-enforcement layer.
