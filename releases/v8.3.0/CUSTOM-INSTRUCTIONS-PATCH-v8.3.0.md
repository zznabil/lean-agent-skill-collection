# Optional V8.3.0 Custom Instructions patch

Replace a generic writing rule with:

```markdown
- For substantial instructions, UI text, errors, help, forms, manuals, or onboarding, identify the intended user and task; state purpose, prerequisites, action, expected result, recovery, and material consequences; use stable terms; expose progress and saved state when relevant; and test the real task. Keep simple replies short. Do not claim Easy-to-Read or cognitive accessibility from a readability score alone.
```
