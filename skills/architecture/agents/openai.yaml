interface:
  display_name: Architecture
  short_description: Design durable boundaries and shared action contracts
  default_prompt: 'Use $architecture to design the smallest durable structure and interface contract for this change. Keep the global adaptive-prose and considerate-agency overlays active: simple turns stay short; heavier structure appears only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
