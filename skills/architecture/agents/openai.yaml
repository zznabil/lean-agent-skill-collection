interface:
  display_name: "Architecture"
  short_description: "Design durable boundaries and shared action contracts"
  default_prompt: "Use $architecture to design the smallest durable structure and interface contract for this change. Keep the global user-facing overlay active for eligible chat prose."
policy:
  products:
    - "CHAT"
    - "CODEX"
  allow_implicit_invocation: true
