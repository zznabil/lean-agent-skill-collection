interface:
  display_name: Browser Automation
  short_description: Automate and QA browser journeys with evidence
  default_prompt: Use $browser-automation to run this authorized browser workflow and verify the visible result. Keep the global user-facing and considerate-agency overlays active for eligible chat prose.
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
