interface:
  display_name: Browser Automation
  short_description: Automate and QA browser journeys with evidence
  default_prompt: 'Use $browser-automation to run this authorized browser workflow and verify the visible result. Keep the global outcome-first, adaptive-prose, and considerate-agency overlays active: match reply weight to the task, report outcome, verification, and remaining action without routine process replay, and use heavier structure only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
