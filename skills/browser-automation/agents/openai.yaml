interface:
  display_name: Browser Automation
  short_description: Automate and QA browser journeys with evidence
  default_prompt: 'Use $browser-automation to run this authorized browser workflow and verify the visible result. Keep the global adaptive-prose and considerate-agency overlays active: simple turns stay short; heavier structure appears only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
