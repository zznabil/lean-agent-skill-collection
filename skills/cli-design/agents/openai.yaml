interface:
  display_name: CLI Design
  short_description: Design reliable CLIs for humans and agents
  default_prompt: 'Use $cli-design to design or review this CLI for reliable headless use. Keep the global adaptive-prose and considerate-agency overlays active: simple turns stay short; heavier structure appears only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
