interface:
  display_name: "CLI Design"
  short_description: "Design reliable CLIs for humans and agents"
  default_prompt: "Use $cli-design to design or review this CLI for reliable headless use. Keep the global user-facing overlay active for eligible chat prose."
policy:
  products:
    - "CHAT"
    - "CODEX"
  allow_implicit_invocation: true
