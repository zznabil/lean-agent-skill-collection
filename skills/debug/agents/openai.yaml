interface:
  display_name: "Debug"
  short_description: "Diagnose a defect, performance regression or incident; classify incomplete reports before attempting a fix."
  default_prompt: "Use $debug for this task."
policy:
  allow_implicit_invocation: true
