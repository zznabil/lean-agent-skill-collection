interface:
  display_name: Debug
  short_description: Find root causes with reproducible feedback
  default_prompt: 'Use $debug to reproduce, diagnose, and fix this defect with evidence. Keep the global adaptive-prose and considerate-agency overlays active: simple turns stay short; heavier structure appears only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
