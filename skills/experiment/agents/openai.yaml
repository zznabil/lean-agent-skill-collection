interface:
  display_name: "Experiment"
  short_description: "Compare a hypothesis or prototype against a controlled baseline with decision-relevant measures and a stopping rule."
  default_prompt: "Use $experiment for this task."
policy:
  allow_implicit_invocation: true
