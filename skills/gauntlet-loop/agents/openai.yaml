interface:
  display_name: "Gauntlet Loop"
  short_description: "Run an explicitly requested bounded adversarial acceptance loop when ordinary verification leaves material hidden-defect risk."
  default_prompt: "Use $gauntlet-loop for this task."
policy:
  allow_implicit_invocation: false
