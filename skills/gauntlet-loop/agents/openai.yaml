interface:
  display_name: Gauntlet Loop
  short_description: Attack, repair, and retest high-risk artifacts
  default_prompt: 'Use $gauntlet-loop to attack this artifact until it passes or reaches a bounded stop. Keep the global adaptive-prose and considerate-agency overlays active: simple turns stay short; heavier structure appears only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: false
