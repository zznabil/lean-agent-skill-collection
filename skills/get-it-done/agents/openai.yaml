interface:
  display_name: "Get It Done"
  short_description: "Own an explicitly requested long-running task through implementation, verification and authorised delivery."
  default_prompt: "Use $get-it-done for this task."
policy:
  allow_implicit_invocation: false
