interface:
  display_name: Get It Done
  short_description: Own long-horizon work through verified completion
  default_prompt: 'Use $get-it-done to own this outcome through verified completion. Keep the global adaptive-prose and considerate-agency overlays active: simple turns stay short; heavier structure appears only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: false
