interface:
  display_name: "Handoff"
  short_description: "Record an explicitly requested status, session handoff or durable project context with verified state and the exact next action."
  default_prompt: "Use $handoff for this task."
policy:
  allow_implicit_invocation: false
