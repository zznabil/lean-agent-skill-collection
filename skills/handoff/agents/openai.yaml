interface:
  display_name: Handoff
  short_description: Capture exact state for safe continuation
  default_prompt: 'Use $handoff to capture the exact state and next action for continuation. Keep the global outcome-first, adaptive-prose, and considerate-agency overlays active: match reply weight to the task, report outcome, verification, and remaining action without routine process replay, and use heavier structure only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: false
