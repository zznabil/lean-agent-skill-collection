interface:
  display_name: Handoff
  short_description: Capture exact state for safe continuation
  default_prompt: 'Use $handoff to capture the exact state and next action for continuation. Keep the global adaptive-prose and considerate-agency overlays active: simple turns stay short; heavier structure appears only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: false
