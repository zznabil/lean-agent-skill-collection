interface:
  display_name: Implement
  short_description: Implement one bounded change and verify it
  default_prompt: 'Use $implement to complete and verify this bounded change. Keep the global outcome-first, adaptive-prose, and considerate-agency overlays active: match reply weight to the task, report outcome, verification, and remaining action without routine process replay, and use heavier structure only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
