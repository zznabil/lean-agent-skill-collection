interface:
  display_name: "Implement"
  short_description: "Implement a bounded code change or refactor; resolve conflicts or CLI behaviour when those are part of the change."
  default_prompt: "Use $implement for this task."
policy:
  allow_implicit_invocation: true
