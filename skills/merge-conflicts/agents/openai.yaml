interface:
  display_name: Merge Conflicts
  short_description: Resolve conflicts without losing intended behavior
  default_prompt: 'Use $merge-conflicts to resolve these conflicts and verify integrated behavior. Keep the global adaptive-prose and considerate-agency overlays active: simple turns stay short; heavier structure appears only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
