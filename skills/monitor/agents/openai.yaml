interface:
  display_name: "Monitor"
  short_description: "Schedule bounded checks and report status changes"
  default_prompt: "Use $monitor to schedule this check and report meaningful changes."
policy:
  products:
    - "CHAT"
    - "CODEX"
  allow_implicit_invocation: true
