interface:
  display_name: "Office Files"
  short_description: "Create and validate office documents and files"
  default_prompt: "Use $office-files to create or repair this file and validate the result. Keep the global user-facing overlay active for eligible chat prose."
policy:
  products:
    - "CHAT"
    - "CODEX"
  allow_implicit_invocation: true
