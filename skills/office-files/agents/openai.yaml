interface:
  display_name: "Office Files"
  short_description: "Create, edit or repair documents, presentations, PDFs and spreadsheets; validate the final file and required visual fidelity."
  default_prompt: "Use $office-files for this task."
policy:
  allow_implicit_invocation: true
