interface:
  display_name: "Plan"
  short_description: "Turn evidence into an executable work plan"
  default_prompt: "Use $plan to turn the current evidence into an executable plan."
policy:
  products:
    - "CHAT"
    - "CODEX"
  allow_implicit_invocation: true
