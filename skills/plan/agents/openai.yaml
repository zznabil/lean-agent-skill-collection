interface:
  display_name: "Plan"
  short_description: "Plan a multi-step change, architecture decision or unresolved requirement; do not implement a plan-only request."
  default_prompt: "Use $plan for this task."
policy:
  allow_implicit_invocation: true
