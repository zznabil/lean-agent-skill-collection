interface:
  display_name: "Project Context"
  short_description: "Capture context, AI asset cards, lessons, or retrospectives"
  default_prompt: "Use $project-context to capture verified context, an AI asset card, approved lessons, or session improvements. Keep the global user-facing overlay active for eligible chat prose."
policy:
  products:
    - "CHAT"
    - "CODEX"
  allow_implicit_invocation: true
