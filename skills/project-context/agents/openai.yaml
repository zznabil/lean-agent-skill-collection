interface:
  display_name: Project Context & Map
  short_description: Map repositories and manage durable context
  default_prompt: 'Use $project-context to create the requested repository map, verified context, AI asset card, approved lesson, or retrospective. Keep the global outcome-first, adaptive-prose, and considerate-agency overlays active: match reply weight to the task, report outcome, verification, and remaining action without routine process replay, and use heavier structure only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: false
