interface:
  display_name: "Release"
  short_description: "Prepare and verify a versioned release; publish and clean up only within the approval already granted."
  default_prompt: "Use $release for this task."
policy:
  allow_implicit_invocation: true
