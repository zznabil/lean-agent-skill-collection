interface:
  display_name: "Repository Map"
  short_description: "Explain verified repository structure and flows"
  default_prompt: "Use $repo-map to explain or map the verified structure and runtime flow of this repository."
policy:
  products:
    - "CHAT"
    - "CODEX"
  allow_implicit_invocation: true
