interface:
  display_name: "Research"
  short_description: "Produce a current, source-backed research brief"
  default_prompt: "Use $research to investigate this question with current primary sources."
policy:
  products:
    - "CHAT"
    - "CODEX"
  allow_implicit_invocation: true
