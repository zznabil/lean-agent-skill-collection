interface:
  display_name: Research
  short_description: Produce a current, source-backed research brief
  default_prompt: 'Use $research to investigate this question with current primary sources. Keep the global adaptive-prose and considerate-agency overlays active: simple turns stay short; heavier structure appears only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
