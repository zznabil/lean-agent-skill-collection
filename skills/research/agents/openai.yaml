interface:
  display_name: Research
  short_description: Produce a current, source-backed research brief
  default_prompt: 'Use $research to investigate this question with current primary sources. Keep the global outcome-first, adaptive-prose, and considerate-agency overlays active: match reply weight to the task, report outcome, verification, and remaining action without routine process replay, and use heavier structure only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
