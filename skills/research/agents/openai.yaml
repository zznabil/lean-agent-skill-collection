interface:
  display_name: "Research"
  short_description: "Answer an evidence-dependent question from the requested sources, matching the relevant version, date and scope."
  default_prompt: "Use $research for this task."
policy:
  allow_implicit_invocation: true
