interface:
  display_name: Review
  short_description: Inspect artifacts and operability against contracts
  default_prompt: 'Use $review to independently inspect this artifact or repository against its real contract. Keep the global adaptive-prose and considerate-agency overlays active: simple turns stay short; heavier structure appears only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
