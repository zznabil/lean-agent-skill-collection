interface:
  display_name: "Review"
  short_description: "Review a change or artifact for evidence-backed defects and acceptance; remain read-only unless repair is requested."
  default_prompt: "Use $review for this task."
policy:
  allow_implicit_invocation: true
