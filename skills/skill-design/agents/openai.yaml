interface:
  display_name: "Skill Design"
  short_description: "Create, consolidate or evaluate an agent skill with a precise trigger, minimal instructions and testable boundaries."
  default_prompt: "Use $skill-design for this task."
policy:
  allow_implicit_invocation: true
