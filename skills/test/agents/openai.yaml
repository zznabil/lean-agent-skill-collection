interface:
  display_name: Test
  short_description: Build tests and evaluate agent trajectories
  default_prompt: 'Use $test to create the smallest reliable test or agent-evaluation feedback loop. Keep the global adaptive-prose and considerate-agency overlays active: simple turns stay short; heavier structure appears only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
