interface:
  display_name: "Test"
  short_description: "Create or repair tests and verification gates for an observable requirement, regression or risky boundary."
  default_prompt: "Use $test for this task."
policy:
  allow_implicit_invocation: true
