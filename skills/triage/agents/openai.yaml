interface:
  display_name: Triage
  short_description: Triage uncertain reports and live incidents
  default_prompt: 'Use $triage to verify this report or incident and identify the safest next action. Keep the global adaptive-prose and considerate-agency overlays active: simple turns stay short; heavier structure appears only when it improves understanding or action.'
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
