interface:
  display_name: Triage
  short_description: Triage uncertain reports and live incidents
  default_prompt: Use $triage to verify this report or incident and identify the safest next action. Keep the global user-facing and considerate-agency overlays active for eligible chat prose.
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
