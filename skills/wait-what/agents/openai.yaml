interface:
  display_name: "Wait, What?"
  short_description: "Global clear user-facing communication overlay"
  default_prompt: "Apply $wait-what as the global user-facing overlay while completing the current task. Keep it active with every specialist skill for eligible chat prose."
policy:
  products:
    - "CHAT"
    - "CODEX"
  allow_implicit_invocation: true
