interface:
  display_name: "Wait What"
  short_description: "Re-explain or simplify a confusing answer when the user explicitly asks for a clearer version."
  default_prompt: "Use $wait-what for this task."
policy:
  allow_implicit_invocation: false
