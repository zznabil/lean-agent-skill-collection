interface:
  display_name: "Writing"
  short_description: "Draft or edit prose for its audience and purpose while preserving source meaning and the requested voice."
  default_prompt: "Use $writing for this task."
policy:
  allow_implicit_invocation: true
